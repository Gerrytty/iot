syntax = "proto3";
option java_multiple_files = true;

message RootRequest {
  int32 number = 1;
}

message RootResponse {
  double root = 1;
}

service RootService {
  rpc root(RootRequest) returns (RootResponse);
}